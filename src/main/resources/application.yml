micronaut:
  application:
    name: expenses
  server:
    cors:
      enabled: true
datasources:
  default:
    driverClassName: com.mysql.cj.jdbc.Driver
    dialect: MYSQL
    url: jdbc:mysql://localhost:3306/expenses
    username: root
    password: "password"
flyway:
  datasources:
    default:
      enabled: true
netty:
  default:
    allocator:
      max-order: 3
